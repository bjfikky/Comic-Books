import React from 'react';

const Navbar = () => {
    return (
        <nav class="navbar navbar-dark bg-dark">
            <span class="navbar-brand mb-0 h1">Comic Books</span>
        </nav>
    );
};

export default Navbar;
